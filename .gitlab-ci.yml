image: docker:stable

variables:
   DOCKER_HOST: tcp://docker:2375/
   DOCKER_DRIVER: overlay2

services:
  - docker:stable-dind

before_script:
  - docker info

test:
  script:
  - echo "Tests"
  - ./tests.sh
  
